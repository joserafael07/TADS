info:
  name: Post
  type: http
  seq: 7

http:
  method: POST
  url: http://localhost:8080/echo
  body:
    type: text
    data: Boa Noite!
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
